% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_pkgs.R
\name{check_pkgs}
\alias{check_pkgs}
\alias{installed_pkgs}
\alias{missing_pkgs}
\alias{install_missing_pkgs}
\alias{update_installed_pkgs}
\alias{find_pkgs}
\alias{check_allowed_pkgs}
\alias{check_disallowed_pkgs}
\alias{check_required_pkgs}
\title{Check for allowed or disallowed package dependencies within a project or directory using \code{pak::scan_deps()}.}
\usage{
installed_pkgs()

missing_pkgs(
  dir = here::here(),
  regexp = "[.](R|r|Rmd|rmd|Rnw|rnw|Qmd|qmd)$",
  recurse = TRUE
)

install_missing_pkgs(
  dir = here::here(),
  regexp = "[.](R|r|Rmd|rmd|Rnw|rnw|Qmd|qmd)$",
  recurse = TRUE,
  ...
)

update_installed_pkgs()

find_pkgs(
  dir = here::here(),
  regexp = "[.](R|r|Rmd|rmd|Rnw|rnw|Qmd|qmd)$",
  full = FALSE,
  recurse = TRUE
)

check_allowed_pkgs(
  pkgs,
  dir = here::here(),
  regexp = "[.](R|r|Rmd|rmd|Rnw|rnw|Qmd|qmd)$",
  recurse = TRUE
)

check_disallowed_pkgs(
  pkgs,
  dir = here::here(),
  regexp = "[.](R|r|Rmd|rmd|Rnw|rnw|Qmd|qmd)$",
  recurse = TRUE
)

check_required_pkgs(
  pkgs,
  dir = here::here(),
  regexp = "[.](R|r|Rmd|rmd|Rnw|rnw|Qmd|qmd)$",
  recurse = TRUE
)
}
\arguments{
\item{dir}{Directory to search.}

\item{regexp}{Regular expression used to select files, defaults to matching
\code{R}, \code{Rmd}, \code{Rnw}, and \code{qmd} files (and their case variants).}

\item{recurse}{Should directory be recursively explored (i.e. match files in sub directories)}

\item{...}{Additional arguments passed to \code{\link[=install.packages]{install.packages()}}.}

\item{full}{Should the full data frame of dependencies be returned or just a vector of package names.}

\item{pkgs}{Character vector of package names}
}
\value{
\code{installed_pkgs()}, \code{missing_pkgs()}, and \code{find_pkgs()} return a
character vector of package names (\code{find_pkgs()} returns a data frame of
dependency details when \code{full = TRUE}). \code{install_missing_pkgs()} and
\code{update_installed_pkgs()} are called for their side effect of installing
or updating packages and return \code{NULL} or the result of
\code{\link[pak:pkg_install]{pak::pkg_install()}} invisibly. The \verb{check_*} functions return \code{TRUE} or
\code{FALSE} invisibly, indicating whether the check passed.
}
\description{
Check for allowed or disallowed package dependencies within a project or directory using \code{pak::scan_deps()}.
}
\section{Functions}{
\itemize{
\item \code{installed_pkgs()}: Returns a vector of installed packages.

\item \code{missing_pkgs()}: Returns a vector of packages found by \code{find_pkgs} that are not currently installed.

\item \code{install_missing_pkgs()}: Installs missing packages found by \code{missing_pkgs}.

\item \code{update_installed_pkgs()}: Updates installed packages using \code{\link[pak:pkg_install]{pak::pkg_install()}}.
Packages installed from a remote such as GitHub are updated from that same
source, others are updated from the configured repositories. Packages that
are not available from a repository and were not installed from a remote
(e.g. packages installed from a local source tarball) are skipped.

\item \code{find_pkgs()}: Find all of the packages used within a project using \code{pak::scan_deps()}.

\item \code{check_allowed_pkgs()}: Check that only the allowed packages are used

\item \code{check_disallowed_pkgs()}: Check if any disallowed packages are used

\item \code{check_required_pkgs()}: Check that the required packages are used

}}
\examples{
\dontrun{
dir = system.file("examples/package", package = "checklist")

find_pkgs(dir)
missing_pkgs(dir)

check_allowed_pkgs(c("dplyr", "ggplot2"), dir)
check_disallowed_pkgs("nlme", dir)
check_required_pkgs("dplyr", dir)
}

}
